include mixins/app-wrapper.pug

+wrapper()
  p Viewing S3 bucket #{bucket}

  ul.objects-list 
    each object in objects
      li
        a(href=`/s3/buckets/${bucket}/objects/${object.Key}`)= object.Key

  if (!objects.length)
    p Such an empty bucket
